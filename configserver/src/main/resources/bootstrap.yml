spring:
  application:
    name: config-server
  profiles:
    active: native
  security:
    user:
      password: admin

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

server:
  port: 8080
